/*define(function () {
    return{
        run:function () {
            alert('ajax');
            function setPage(_url) {
                var xhr = new XMLHttpRequest();
                xhr.open('get', _url);
                xhr.send(null);
                xhr.addEventListener('readystatechange', function () {
                    if (xhr.readyState == 4) {
                        if (xhr.status == 200) {

                        }
                    }
                })
            }
        }

    }
});*/
